#  Ya esxiste la variable de sesion sIdUsuario por ello la guardamos
#  para evitar conflictos con la variable que crea el sistema al actualizar registros
#  y le devolvemos su valor al finalizar la ejecucion del archivo
#  Este proceso se debe repetir en quellos archivos donde se use el campo sIdUsuario como
#  llave primaria para ABC de datos
